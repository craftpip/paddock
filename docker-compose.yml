services:
  vm-ozden:
    build:
      context: ./vm_openclaw
    image: vm-friends-vm-openclaw:latest
    container_name: vm-ozden
    restart: unless-stopped
    volumes:
      - ./instances/vm-ozden/openclaw:/root/.openclaw
      - /tmp/screenshots:/tmp/screenshots
    environment:
      TZ: Asia/Kolkata
      ROOT_PASSWORD: ozden

